@model IEnumerable<AdSite.Models.CRUDModels.LocalizationViewModel>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.LocalizationKey)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.LocalizationValue)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Language.Name)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.LocalizationKey)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LocalizationValue)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Language.Name)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
